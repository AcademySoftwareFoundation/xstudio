// SPDX-License-Identifier: Apache-2.0
#include <gtest/gtest.h>


// using namespace xstudio::utility;
// using namespace xstudio::timeline;

TEST(FrameTest, Test) {
    // EXPECT_TRUE(s2 == s);

    // EXPECT_EQ(to_seconds(Rate(0)), 0);

    //    EXPECT_TRUE(Rate(1));
    //    EXPECT_FALSE(Rate(0));
}
